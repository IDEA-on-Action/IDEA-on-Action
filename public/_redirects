# Cloudflare Pages SPA 라우팅 설정
# IDEA on Action - React Router 지원

# 모든 요청을 index.html로 라우팅 (SPA)
# 정적 파일은 자동으로 서빙됨
/*    /index.html   200

# API 프록시 (Phase 2에서 Workers로 대체 예정)
# /api/*  https://zykjdneewbzyazfukzyg.supabase.co/functions/v1/:splat  200

# 레거시 URL 리다이렉트 (필요시 추가)
# /old-path  /new-path  301
