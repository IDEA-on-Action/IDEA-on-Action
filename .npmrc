# Allow legacy peer dependencies
# Required for @tosspayments packages that require TypeScript 4.x
# while project uses TypeScript 5.8.3
legacy-peer-deps=true

# Lockfile version (npm v7+)
lockfile-version=3
